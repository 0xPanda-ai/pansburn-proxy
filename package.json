{
  "name": "pansburn-proxy",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "echo 'static frontend, use http-server to preview' && exit 1"
  },
  "dependencies": {
    "@vercel/speed-insights": "^1.2.0"
  }
}
